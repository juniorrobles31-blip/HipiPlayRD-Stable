{
  "name": "juega123-modern-platform",
  "private": true,
  "version": "1.0.0",
  "description": "Plataforma moderna PWA para juegos, auditoría blockchain-ready y pool de invitación.",
  "scripts": {
    "install:all": "npm install && npm --prefix apps/api install && npm --prefix apps/web install",
    "dev": "concurrently \"npm --prefix apps/api run dev\" \"npm --prefix apps/web run dev\"",
    "build": "npm --prefix apps/api run build && npm --prefix apps/web run build",
    "start": "npm --prefix apps/api start"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.2",
    "@types/node": "^25.9.1",
    "concurrently": "^9.0.0",
    "hardhat": "^2.28.6",
    "sharp": "^0.34.5",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "ethers": "^6.16.0"
  }
}
